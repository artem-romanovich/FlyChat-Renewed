/*
package com.example.flychat.notification;

import okhttp3.ResponseBody;
import retrofit2.http.Body;
import retrofit2.http.Headers;
import retrofit2.http.POST;

import static com.example.flychat.notification.TestActivity.CONTENT_TYPE;
import static com.example.flychat.notification.TestActivity.SERVER_KEY;

public interface NotificationAPI {

    @Headers({"Authorization: key=" + SERVER_KEY, "Content-Type:" + CONTENT_TYPE})
    @POST("fcm/send")
    static ResponseBody postNotification(@Body PushNotification notification) {
        ResponseBody responseBody = response.body;
        return responseBody;
    }
}
*/
